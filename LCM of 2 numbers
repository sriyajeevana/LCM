#include<stdio.h>
#include<conio.h>
int main()

{

int a,b,L;

printf("Enter two numbers to get LCM of : \n");

scanf("%d%d",&a,&b);

for(L=(a>b?a:b);L<=a*b;L++)

if(L%a==0&&L%b==0)

break;

printf("LCM of 'a' and 'b' is : %d ",L);

}
